apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: referrals-api
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: referrals-api
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
      scrapeTimeout: 10s
