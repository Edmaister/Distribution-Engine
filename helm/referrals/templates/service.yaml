apiVersion: v1
kind: Service
metadata:
  name: {{ include "referrals.fullname" . }}
  labels:
    {{- include "referrals.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "referrals.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
