"""Periodic job (skeleton).
Scans for referrals that became eligible recently and applies rewards.
Can be scheduled via cron/K8s.
"""
def run_once():
    # Example placeholder: query referrals without rewards but with events present.
    # Then call services.reward_service.apply_reward(...)
    pass
